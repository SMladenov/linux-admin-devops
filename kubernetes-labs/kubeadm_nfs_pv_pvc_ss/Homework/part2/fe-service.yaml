apiVersion: v1
kind: Service
metadata:
  name: consumer
  labels:
    app: fun-facts
    role: consumer
spec:
  type: NodePort
  selector:
    app: fun-facts
    role: consumer
  ports:
  - port: 5000
    nodePort: 30001
    protocol: TCP
